name: dashed_circular_progress_bar
description: Dashed circular progress bar for flutter apps.
version: 0.0.2
homepage: https://github.com/radnive/Flutter_DashedCircularProgressBar

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
